import { DinamicaImgBackgroundDirective } from './dinamica-img-background.directive';

describe('DinamicaImgBackgroundDirective', () => {
  it('should create an instance', () => {
    const directive = new DinamicaImgBackgroundDirective();
    expect(directive).toBeTruthy();
  });
});
